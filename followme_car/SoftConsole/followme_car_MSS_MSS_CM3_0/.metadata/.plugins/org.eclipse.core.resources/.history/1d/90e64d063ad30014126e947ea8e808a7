#include <inttypes.h>
#include "bool.h"

#include "car.h"

int main()
{
	int count = 0;
	bool fwd = true;
	while( 1 )
	{
		if (count++ >= 2000000) {
			if (fwd)
				fwd = 0;
			else
				fwd = 1;
			count = 0;
		}
		moveCar(fwd, !fwd, 30, 30);
	}
}
